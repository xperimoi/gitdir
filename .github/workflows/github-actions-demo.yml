name: Test valid licence yaml
on: [push]

inputs:
  files:
    description: 'Yaml files to validate'
    required: true
  schema:
    description: 'Schema definition file'
    required: true

runs: 
  using: 'Dockerfile'
  image: 'licvalidator/licvalidator:latest'
  args:
    - ${{ input.files }}
