name: Test valid licence yaml
on: [push]

jobs: 
  simple_job:
    runs-on: self-hosted
    steps:
      - name: simple step
        id: hellop
        uses: 'docker://licvalidator/licvalidator:v2'
        with:
          files: 'file1.yaml'
