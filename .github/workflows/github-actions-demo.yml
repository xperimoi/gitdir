name: Test valid licence yaml
on: [push]

inputs:
  files:
    description: 'Yaml files to validate'
    required: true
  schema:
    description: 'Schema definition file'
    required: true

jobs: 
  simple_job:
    runs-on: ubuntu-latest
    steps:
      - name: simple step
        id: hellop
        uses: licvalidator/licvalidator:latest
        with:
          files: 'file1.yaml'
